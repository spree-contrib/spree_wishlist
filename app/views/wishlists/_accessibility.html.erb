<% form_for @wishlist, :html => {:class => 'ajax_form', :id => 'change_wishlist_accessibility'} do |f| %>
  <%= f.radio_button :is_private, @wishlist.is_private? %>&nbsp;<%= t(:private) %>
  <%= f.radio_button :is_private, !@wishlist.is_private? %>&nbsp;<%= t(:public) %>
<% end -%>
