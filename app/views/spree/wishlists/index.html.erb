<h1 class="row"><%= t(:wishlists) %></h1>
<%= content_tag :ul do %>
  <% @wishlists.each do |wishlist| %>
    <%= content_tag :li do %> 
      <%=  link_to(wishlist.name, wishlist) %>  - <%= wishlist.is_private ? t(:private) : t(:public)%>
    <% end %>
  <% end %>
<% end %>